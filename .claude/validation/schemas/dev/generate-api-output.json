{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Generate API Output Schema",
  "type": "object",
  "required": ["generation_status"],
  "properties": {
    "generation_status": {"type": "string", "enum": ["success", "failed"]},
    "endpoints_created": {"type": "integer", "minimum": 1}
  }
}
