# Behavior Adjustment

Provides the `behavior_adjustment` tool, which merges requested adjustments into the agent's persistent behavior rules file using the Utility Model, and injects the resulting ruleset at the top of the system prompt.
