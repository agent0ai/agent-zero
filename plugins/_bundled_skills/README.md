# Bundled Skills

Bundled Skills is a built-in Agent Zero plugin that carries the bundled skill collection as a standalone skill discovery root.

## What It Does

- keeps the bundled skills discoverable through the standard plugin skill-root scan while enabled
- owns no runtime behavior; loading and visibility stay in the `_skills` plugin
