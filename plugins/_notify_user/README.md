# Notify User

Provides the `notify_user` tool, which sends out-of-band notifications through the Agent Zero notification manager without ending the current task.

Supports message, title, detail, notification type (`info`, `success`, `warning`, `error`, `progress`), priority (20 high, 10 normal), and a display timeout. Agents notify with high priority by default.
